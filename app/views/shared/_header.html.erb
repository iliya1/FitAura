<nav class="top-bar" data-topbar role="navigation">
  <ul class="title-area">
    <li class="name">
      <h1><a href="/">FitAura</a></h1>
    </li>
     <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>

  <section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="right">
    <% if user_signed_in? %>
      <li>
        <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>        
      </li>
    <% elsif studio_signed_in? %>
      <li>
        <%= link_to('Logout', destroy_studio_session_path, :method => :delete) %>        
      </li>
    <% elsif trainer_signed_in? %>
      <li>
        <%= link_to('Logout', destroy_trainer_session_path, :method => :delete) %>        
      </li>

    <% else %>
      <li class="has-dropdown">
        <a href="#">Login</a>
        <ul class="dropdown">
          <li> <%= link_to('User Login', new_user_session_path)  %>  </li>
          <li> <%= link_to('Studio Login', new_studio_session_path)  %>  </li>
          <li> <%= link_to('Trainer Login', new_trainer_session_path)  %>  </li>
        </ul>
      </li>
    <% end %>
    </ul>
  </section>
</nav>
